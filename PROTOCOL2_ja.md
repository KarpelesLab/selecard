# セレカード II（STX9531C）— RF プロトコル

*[English](PROTOCOL2.md) · 日本語*

文化シヤッターの**セレカード II** ガレージシャッター用リモコン（初期の約315 MHz世代。
426 MHzのセレカード III は [`PROTOCOL3_ja.md`](PROTOCOL3_ja.md)）を、電波の受信のみから
独立に解析したものです。例にはすべて合成の ID **`01234567`** を使用し、実在のデバイス ID は
本リポジトリのどこにも登場しません。

表記：ビット列は**送信順（左が先頭）**で記します。III と異なり、II はすべて **MSB
ファースト**で送信します。`nibXOR(v)` は `v` の各16進ニブルの XOR を表します。

## 1. 物理層

| 項目 | 値 |
|---|---|
| 搬送波 | 約315.011 MHz |
| 変調 | **OOK / ASK**（搬送波のオン・オフ）。FSK ではない |
| 線符号 | マンチェスタ |
| ハーフビット周期 | 約802 µs（≒ 312 bit/s） |
| 送信出力 | ≤ 1 mW（微弱無線） |

OOK でのマンチェスタ：各ビットは ON パルスの位置で読みます。データビット `1` → `ON,OFF`
（前半のハーフビットが ON）、データビット `0` → `OFF,ON`。ボタンを押している間フレームが
繰り返され、短く1回押すと1〜2フレームです。

## 2. フレーム

**67ビット**、MSB ファースト：

```
[ preamble ][ header ][ 24-bit ID ][ cmd ][ ctr_lo ][ ctr_hi ][ cksum ]
    16          7          24         4        8         4        4
 1010101010101011  0010001                nibble   low 8    high 4
```

### ID

カードに印刷された8桁の番号は `0 … 16777215` の**10進整数**で、24ビット値として
**MSB ファースト**で送信されます（III は LSB ファーストで送信します。これが両モデルの
ID エンコードにおける唯一の相違点です）。

例 — ID `01234567`：

```
decimal 1234567 = 0x12D687
ID field (MSB-first, as sent) : 000100101101011010000111
```

### コマンド

4ビットフィールドの**上位2ビットに入る2ビットコード**です（下位2ビットは常に `0`）：

| コマンド | フィールド `[47:51]` | ニブル |
|---|---|---|
| OPEN（開） | `0100` | 4 |
| CLOSE（閉） | `1000` | 8 |
| STOP（停止） | `1100` | 12 |

### カウンタ

**12ビットのカウンタ** `FC`。フレーム内で分割され、下位側が先に送られます。
`ctr_lo` = ビット `[51:59]`（下位8ビット）、`ctr_hi` = ビット `[59:63]`（上位4ビット）で、
`FC = ctr_hi<<8 | ctr_lo`。送信フレームごとに +1 され、4096 で一周します。

これが II 唯一のリプレイ対策です（§3）。

### チェックサム

4ビットの末尾 `[63:67]` は、ニブル XOR の畳み込みを2の補数で反転したものです：

```
cksum = ( -( nibXOR(FC) ^ cmd ^ nibXOR(ID) ^ 7 ) ) & 0xF
```

`cmd` はコマンドニブル（4/8/12）、`7` はファミリ共通の固定シード、ID はそれ自身の
ニブル XOR 畳み込みとして入ります。したがって III と同様、有効なフレームは印刷された
ID だけから計算できます。（下位ビットは線形／XOR ですが、末尾の反転により上位ビットに
桁上げが生じます。純粋な XOR モデルでは一部しか合わないのはこのためです。）

例 — ID `01234567`、OPEN、`FC = 0x100`（ctr_lo `0x00`、ctr_hi `0x1`）：

```
nibXOR(0x12D687) = 7      nibXOR(0x100) = 1
cksum = ( -( 1 ^ 4 ^ 7 ^ 7 ) ) & 0xF = ( -5 ) & 0xF = 0xB = 1011
```

## 3. カウンタ／前進ウィンドウ（II のリプレイ対策）

固定コードの III と違い、受信機はカード ID ごとに**最後に受け付けたカウンタ値**を記憶し、
新しいフレームのカウンタがその値より**前**であり、かつ前進ウィンドウ内にある場合のみ
受け付けます：

```
accept  iff  (counter - last_seen) mod 4096  in  [1 .. W]
```

ウィンドウ `W` は実機で測定した結果 **255**（0xFF）でした。4096 のカウンタ空間のうちの
255 です。受け付けると、受信機は `last_seen` を新しいカウンタ値に更新します。

**セキュリティ上の帰結。** `W = 255 / 4096` は弱い保護です。**古い**フレーム（受信機が
すでに通過したカウンタ）の単純なリプレイは拒否しますが、信号を**合成できる**攻撃者は
止められません。カウンタを 0 → 4095 と掃引すれば必ずウィンドウに入り、いったん入れば
カウンタを `last_seen + 1` に保つだけで、周期的な一周（wrap）をまたいでも有効であり続け
ます。`selecard2.py` はまさにこれを行います。`resync` で掃引してウィンドウに入り、以降の
通常操作では `last_seen + 1` を追跡します。

**カウンタの進む速さと実カードの同期ずれ。** ボタンを押し続けると、フレームはおよそ
**169 ms ごと**に繰り返され、カウンタは押している間**毎秒約 5.9** 進みます。これは
**実物のカード**で重要です。受信機の**電波範囲外**でカードを繰り返し押したり押し続けたり
すると、カード側のカウンタだけが進み、受信機の `last_seen` は止まったままになります。差が
`W = 255` を超えると、カードはウィンドウの外に出て動かなくなります。判定は 4096 で一周
するため、復旧するにはカードを**電波範囲内に戻し、カウンタが一周して下側からウィンドウに
再び入るまでボタンを押し続けます**。最悪の場合はほぼ一周、すなわち**約11分**の連続押し
（ウィンドウをわずかに超えてずれたカード）で、大きくずれているほど短時間で済みます。
実用的な助言：**範囲外で操作しないこと**。（`selecard2.py resync` はこの合成版で、範囲全体を
一度に掃引します。）

## 4. 解析方法（概要）

HackRF で 2 MS/s で録音し、搬送波へ周波数変換して低域通過フィルタで OOK 包絡線を取り出し、
ON パルスの位置とフレームごとのクロック推定でマンチェスタ復号しました。ID モデルはドナー
カードに印刷された番号と照合して確認しました。チェックサムは**カウンタの全範囲**を掃引して
解きました。狭い範囲だけに合う式は過適合であることが、ロールオーバ録音（カウンタが
255→0 に一周するまでボタンを押し続けたもの）によって判明し、カウンタの上位ニブルと真の
畳み込みが明らかになりました。ID 項は2枚目のカードで確認し、これらの式のみから組み立てた
フレーム（掃引してから追跡する前進カウンタ付き）で、実際のシャッターを最初から最後まで
操作できました。
