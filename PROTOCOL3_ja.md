# セレカード III（STX0031）— RF プロトコル

*[English](PROTOCOL3.md) · 日本語*

文化シヤッターのセレカード III ガレージシャッター用リモコンを、電波の受信のみから独立に
解析したものです（315 MHzのセレカード II は [`PROTOCOL2_ja.md`](PROTOCOL2_ja.md)）。
以下の例にはすべて合成の ID **`01234567`** を使用し、実在のデバイス ID は本リポジトリの
どこにも登場しません。

表記：ビット列は**送信順（左が先頭）**で記します。ID とチェックサムは **LSB ファースト**で
運ばれます。`fold(x) = (x & 0xFFFF) + (x >> 16)`。

## 1. 物理層

| 項目 | 値 |
|---|---|
| 搬送波 | 426.0737 MHz（426 MHz帯 ARIB STD-T67 テレコントロール副帯域） |
| 変調 | 2-FSK、包絡線一定 |
| 周波数偏移 | 約4 kHz（論理0 ≈ 搬送波+0.1 kHz、論理1 ≈ 搬送波+4.2 kHz） |
| 線符号 | マンチェスタ |
| ハーフビット周期 | 830 µs（≒ 602 bit/s） |
| 送信出力 | ≤ 1 mW（微弱無線） |

1回の操作で **3回連続で繰り返されるフレーム**が1バーストとして送信されます。各フレームは、
先頭の交互ハーフビット列（**プリアンブル**、約64ハーフビット、ハーフビット速度の方形波）、
マンチェスタ違反の**区切り（デリミタ）**、続いてマンチェスタ符号化された**データ**から
なります。

本書のマンチェスタの規約：データビット `1` → ハーフビット `10`、データビット `0` → `01`。

## 2. フレーム

データペイロードは **50ビット**です：

```
[ 000 ][ 24-bit ID, LSB-first ][ 8-bit command ][ 15-bit checksum ]
  3            24                     8                15
```

### ID

カードに印刷された8桁の番号は `0 … 16777215`（= 2²⁴−1、すなわち最大8桁）の**10進整数**です。
8進数ではありません（実在のカードには 8 や 9 の数字が含まれます）。24ビット値として
**LSB ファースト**で送信されます。

例 — ID `01234567`：

```
decimal 1234567 = 0x12D687
24-bit, MSB-first : 0001 0010 1101 0110 1000 0111
ID field (LSB-first, as sent) : 111000010110101101001000
```

### コマンド

8ビットの**ワンホット**で、立っているビットがボタンを選びます：

| コマンド | フィールド `[27:35]` |
|---|---|
| OPEN（開） | `00010000` |
| STOP（停止） | `00100000` |
| CLOSE（閉） | `01000000` |
| REGISTER（登録） | `10000000`（隠しの登録ボタン） |

### チェックサム

15ビットの末尾 `[35:50]` は、コマンドをワンホットの加算として混ぜ込んだ
**畳み込み加算チェックサム**です：

```
K = { OPEN: 2^11, STOP: 2^10, CLOSE: 2^9, REGISTER: 2^8 }
checksum = ( fold(ID) + K_command ) mod 2^15        # LSB ファーストで記録
```

例 — ID `01234567`、OPEN：

```
fold(1234567)          = 54937
(54937 + 2^11) mod 2^15 = 56985
56985, 15-bit LSB-first = 100110010111101   <- チェックサムフィールド
```

これは単純な加算チェックで、暗号 MAC ではありません。したがって有効なフレームは印刷された
ID だけから計算できます。

## 3. 固定コード／リプレイ

**前進カウンタはありません**。同じボタンは常に同じフレームを送ります。よってフレームは
そのまま再送可能で、ID から合成できます。`selecard3.py` は ID からフレームを一から組み立て、
（`--tx` を付ければ）送信します。

## 4. 登録（無線でのカード追加）

新しいカードは、シャッターの電波範囲内で、**すでに登録済みのカード**によって無線登録します：

1. REGISTER を **3回**押す → カードが約1秒鳴る（登録モードに入る）。
2. 新カードの8桁を入力：桁の値 *N* に対して **OPEN ×N**、桁を進めるのに **STOP**、
   やり直しに **CLOSE**。数字は 0〜9 で、ID は10進です。
3. REGISTER を **1回**押して確定。

送信されるのは2回の REGISTER 押下のみで、数字入力は**無線的に無音**です（カード側で
局所的に数えます）。確定パケットは、4つの24ビットフィールドを運ぶ長いフレームです：

```
[ own-ID ][ A = check(own-ID) ][ new-ID ][ C = check(new-ID) ]
```

`A` と `C` は**同じ fold** を用い、シードだけが異なります：

```
A = ( 0x800080 + 256 * fold(own_ID) ) mod 2^24     # LSB ファースト
C = ( 0xFF00FF + 256 * fold(new_ID) ) mod 2^24     # LSB ファースト
```

これらも単純な加算チェックであるため、登録パケットも ID だけから計算できます。本方式は
どの層にも暗号的な保護を持ちません。

## 5. 解析方法（概要）

HackRF で 2 MS/s で録音し、搬送波へチャネライズして、ヒステリシススライサとドリフト追従の
ハーフビットクロックで FSK 復調し、マンチェスタ復号しました。ID モデルは復号フレームを
ドナーカードに印刷された番号と照合して確認しました。チェックサムは加算として解きました
（XOR／線形モデルは、保留していた値で反証されます。`ID+1` と `ID+65536` は同じチェックサム
ビットを反転し、両方を設定すると次のビットへ**桁上げ**します。これは XOR ではなく加算の
特徴です）。コマンド合成は、これらの式だけから組み立てたフレームで実際のシャッターを操作
して検証しました。
